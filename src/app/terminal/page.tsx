import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Terminal — axjns.dev",
  description: "The old axjns.dev terminal, preserved as an easter egg.",
};

// ClientLayout renders the full TerminalShell for this route; children are unused.
export default function TerminalPage() {
  return null;
}
